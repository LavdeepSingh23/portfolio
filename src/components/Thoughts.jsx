export default function Thoughts() {
  return (
    <section id="thoughts" className="thoughts">
      {/* TODO: "Currently thinking about" cards — placeholder until blog exists */}
      <p style={{ color: 'var(--color-text-secondary)', padding: '2rem 1rem' }}>Thoughts / Curiosities</p>
    </section>
  )
}
