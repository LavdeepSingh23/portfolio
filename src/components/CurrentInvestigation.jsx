export default function CurrentInvestigation() {
  return (
    <div className="current-investigation">
      {/* TODO: What you're currently building / reading / learning */}
      {/* 3 short lines max — only if you'll update monthly */}
      <p style={{ color: 'var(--color-text-secondary)', padding: '1rem' }}>Current Investigation</p>
    </div>
  )
}
