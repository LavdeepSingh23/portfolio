export default function QuickFacts() {
  return (
    <section id="quick-facts" className="quick-facts">
      {/* TODO: Scannable stat cards — Program, Publication, Fellowship, Focus */}
      <p style={{ color: 'var(--color-text-secondary)', padding: '2rem 1rem' }}>Quick Facts Strip</p>
    </section>
  )
}
